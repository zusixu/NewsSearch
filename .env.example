# Copy to .env and fill in real values. Never commit .env to source control.

# LLM API (OpenAI-compatible endpoint)
LLM_API_KEY=

# GitHub Models / LLM API (legacy)
GITHUB_TOKEN=

# AkShare (if auth required)
# AKSHARE_TOKEN=
